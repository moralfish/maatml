{
  "schema_version": "1",
  "system": "You are Flow Studio's local workflow-planning agent. Return only valid JSON matching the response schema. Plan bounded Flow Studio actions, prefer deterministic tool calls, and include Flow DSL or a DSL patch only when the request is specific enough. If the request is ambiguous, unsafe, or unsupported, set refusal_reason and keep confidence below 0.7.",
  "user_template": "Agent input:\n<<AGENT_INPUT>>\n\nReturn one strict JSON object.",
  "response_schema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["intent_summary", "plan_steps", "tool_calls", "dsl_patch", "dsl", "confidence", "refusal_reason"],
    "properties": {
      "intent_summary": { "type": "string" },
      "plan_steps": {
        "type": "array",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": ["id", "title", "action", "depends_on"],
          "properties": {
            "id": { "type": "string" },
            "title": { "type": "string" },
            "action": { "type": "string" },
            "depends_on": {
              "type": "array",
              "items": { "type": "string" }
            }
          }
        }
      },
      "tool_calls": {
        "type": "array",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": ["name", "arguments"],
          "properties": {
            "name": { "type": "string" },
            "arguments": { "type": "object" }
          }
        }
      },
      "dsl_patch": { "type": ["string", "null"] },
      "dsl": { "type": ["string", "null"] },
      "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
      "refusal_reason": { "type": ["string", "null"] }
    }
  },
  "json_keys_order": ["intent_summary", "plan_steps", "tool_calls", "dsl_patch", "dsl", "confidence", "refusal_reason"],
  "max_new_tokens": 768,
  "temperature": 0.0,
  "candidate_models": {
    "primary": "Qwen/Qwen3-4B-Instruct-2507",
    "fallback": "HuggingFaceTB/SmolLM3-3B",
    "baselines": [
      "microsoft/Phi-4-mini-instruct",
      "meta-llama/Llama-3.2-3B-Instruct",
      "google/gemma-3-1b-it",
      "HuggingFaceTB/SmolLM2-360M-Instruct"
    ]
  }
}
