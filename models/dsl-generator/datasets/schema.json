{
  "name": "dsl_generator_sample",
  "description": "Plain-English description of a Flow Studio flow paired with the canonical Flow DSL document that realises it. Used to fine-tune a generative model that maps user prose to DSL. The packaged model's response schema (prompt_spec.response_schema) is the runtime contract; this file describes the on-disk training sample shape.",
  "fields": {
    "sample_id": "string",
    "source": "string",
    "description": "string",
    "dsl": "string",
    "split": "string"
  }
}
